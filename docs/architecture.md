# Architecture

_Empty on purpose. This file describes how the app ACTUALLY works — it gets
written only after `PRD.md` exists, by translating the plan into a concrete
structure, and it MUST be updated whenever the app changes._

_This is a beginner project: keep everything simple and understandable.
If the architecture needs a diagram with more than five boxes, it's too
complicated — simplify the app instead._

<!-- Agent: after the PRD is agreed, describe here (short!):
     - the files and what each one does
     - how data flows (input → state → what gets redrawn)
     - decisions taken and why (one line each) -->
